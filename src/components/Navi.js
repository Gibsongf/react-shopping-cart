import { useNavigate } from "react-router-dom";

const Navi = () => {
	const navigate = useNavigate();
	// the theme logo at far right
	// home button/link (this start grey as being default selected)
	// shop button
	return (
    <div className="header">
        <div className="nav">
            
            <ul>
                <li></li>
            </ul>
        </div>
    </div>
    );
};
